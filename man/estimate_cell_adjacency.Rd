% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admix_scores.R
\name{estimate_cell_adjacency}
\alias{estimate_cell_adjacency}
\title{Estimate cell–cell adjacency via 2D Delaunay on cell centroids}
\usage{
estimate_cell_adjacency(
  df.spatial,
  random.shift = 0.001,
  edge.max.mad = 4,
  n.cores = 1
)
}
\arguments{
\item{df.spatial}{Data frame with \code{cell}, \code{x}, \code{y}, and \code{celltype} or
\code{cell_type} (molecule-level or one row per cell).}

\item{random.shift}{Uniform jitter added to coordinates before triangulation
(default \code{1e-3}).}

\item{edge.max.mad}{Keep edges with distance below
\code{median(dist) + edge.max.mad * mad(dist)} (default \code{4}).}

\item{n.cores}{Number of cores for tiled triangulation (default \code{1}).}
}
\value{
Data frame of directed cell–cell edges with columns
\code{cell_s}, \code{cell_e}, \code{cts}, \code{cte}.
}
\description{
Transcripts are first collapsed to one point per cell with
\code{\link[=cells_from_transcripts]{cells_from_transcripts()}} (mean \code{x}/\code{y}), then a Delaunay graph is built
and filtered by edge length. Accepts molecule-level or already-collapsed
tables with \code{celltype} or \code{cell_type}.
}
