% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admix_scores.R
\name{cells_from_transcripts}
\alias{cells_from_transcripts}
\title{Collapse transcript coordinates to one row per cell}
\usage{
cells_from_transcripts(df.spatial)
}
\arguments{
\item{df.spatial}{Data frame of transcripts with at least \code{cell}, \code{x}, \code{y},
and a cell-type column named \code{celltype} or \code{cell_type}. Optional \code{z} is
averaged when present.}
}
\value{
A data frame with one row per cell: \code{cell}, \code{x}, \code{y}, \code{celltype},
\code{cell_type} (same labels; for compatibility with both naming conventions),
and \code{z} if it was present in the input.
}
\description{
Computes 2D cell centroids as the mean transcript \code{x}/\code{y} per \code{cell}
(and mean \code{z} when present). Called by default inside
\code{\link{estimate_cell_adjacency}} before Delaunay triangulation.
}
